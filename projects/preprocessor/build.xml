<?xml version="1.0" encoding="utf-8"?>
<project name="preprocessor" default="init">

	<!--
	   to make a SVN revision number by using TortoiseSVN installed.
	   google with "TortoiseSVN SubWCRev" for detail.
	-->
    <target name="revision">
        <echo file="rev.tmp" append="false"><![CDATA[----------------------------------------------------------------------------
$Id: PHP Preprocessor, written by Ksnk (sergekoriakin@gmail.com)
Ver : 1.1, Rev: $WCREV$, Modified: $WCDATE$$
      $WCURL$
----------------------------------------------------------------------------
License GNU/LGPL - Serge Koriakin - Jule 2010
----------------------------------------------------------------------------
]]>        </echo>
         <exec executable="SubWCRev" dir=".">
          <arg file="." />
             <arg file="rev.tmp" />
             <arg file="rev.tmp" />
             <arg value="-f" />
          </exec>
        <loadfile property="svn.lastrevision" file="rev.tmp"/>

        <echo msg="revision=${svn.lastrevision}"/>
    </target>

	<target name="init" depends="revision">
        <property file="../env.prop"/>
        <property name="target" value="release"/>
        <property name="dst" value="build"/>
        <property name="config" value="config.xml"/>
        <exec
            command="${php_exe} -q  ../preprocessor/preprocessor.php /Dtarget=${target} /Ddst=${dst} ${config}"
            checkreturn="true"
            logoutput="true"/>

        <delete file="rev.tmp"/>
<!--		<zip duplicate="preserve" destfile="./preprocessor.zip">
			<fileset dir="build">
				<include name="**/*.*"/>
			</fileset>
		</zip>-->
	</target>

	<target name="test">
		<preprocess dst="build" config="test/config.xml"/>
	</target>
	
</project>